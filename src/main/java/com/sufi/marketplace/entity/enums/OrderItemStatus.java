package com.sufi.marketplace.entity.enums;

public enum OrderItemStatus {
    PLACED,
    CONFIRMED,
    PACKED,
    SHIPPED,
    DELIVERED,
    CANCEL_REQUESTED,
    CANCELLED,
    RETURN_REQUESTED,
    APPROVED,
    RETURNED,
    REFUNDED
}
