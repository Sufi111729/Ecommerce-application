package com.sufi.marketplace.entity.enums;

public enum OrderStatus {
    PLACED,
    CONFIRMED,
    PACKED,
    SHIPPED,
    DELIVERED,
    CANCEL_REQUESTED,
    CANCELLED,
    RETURN_REQUESTED,
    APPROVED,
    RETURNED,
    REFUNDED
}
